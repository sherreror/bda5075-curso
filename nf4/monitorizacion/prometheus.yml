global:
  scrape_interval: 5s
  evaluation_interval: 5s
rule_files:
  - /etc/prometheus/alertas.yml
scrape_configs:
  - job_name: pipeline_torre_control
    static_configs:
      - targets: ["host.docker.internal:8000"]   # el pipeline del alumno
